import React, { Fragment } from 'react'

function Spinner() {
    return (
        <Fragment>
            <img src={spinner} alt="Fetching Data" style ={{ width}} />
        </Fragment>
    )
}

export default Spinner