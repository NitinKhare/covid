import React, { Fragment } from 'react'

function Spinner() {
    return (
        <Fragment>
            <img src={spinner} alt="Fetching Data" style ={{} />}
        </Fragment>
    )
}

export default Spinner